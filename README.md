# local-tts-service
A local Text-to-Speech service that:  - Provides an HTTP API for agents to generate speech - Supports voice cloning (F5-TTS) - Manages a versioned voice library - Integrates with the home gateway + agent system - Runs entirely locally (no external APIs)
